import React from 'react'
import { Text, View } from 'react-native'
import Svg, { Path } from "react-native-svg"

export const CreateCountScreen = () => {
  return (
    <View>
        <Text> Create Count </Text>
    </View>
  )
}
